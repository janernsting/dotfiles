[user]
  email = jan.ernsting@gmail.com
  name = Jan Ernsting
[giggle]
  main-window-maximized = false
  main-window-geometry = 1328x999+66+82
  history-view-vpane-position = 681
  main-window-view = HistoryView
  show-graph = true
  file-view-vpane-position = 663
[core]
  editor = vi
  excludesfile = ~/.gitignore_global
  autocrlf = input
[color]
  ui = auto
  branch = auto
  diff = auto
  interactive = auto
  status = auto
[alias]
  st = status
  ci = commit
  br = branch
  co = checkout
  df = diff
  dfc = diff --cached
  lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
  graph = log --graph --decorate --oneline
  who = shortlog -s --
  wdf = diff --word-diff
  wdfc = diff --word-diff --cached
  serve = !git daemon --reuseaddr --verbose --base-path=. --export-all ./.git
[url "ssh://git@github.com/"]
  insteadOf = gh:
  insteadOf = gh:~
[rebase]
  autosquash = true
[rerere]
  enabled = true
[push]
	default = simple
[pull]
	ff = only
	rebase = true
